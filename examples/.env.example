# Example environment file.
# Safe to commit only because all values are placeholders.
# Do not put real production secrets in this file.

APP_ENV=development
API_BASE_URL=http://localhost:3000
DATABASE_URL=postgres://user:password@localhost:5432/app_dev
EXTERNAL_API_KEY=replace-with-runtime-secret
SESSION_SECRET=replace-with-runtime-secret

# Projects should load real secrets through the runtime secret handling model.
